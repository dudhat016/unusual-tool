import { FeatureFlag } from '../types/admin';

// Feature flags are managed dynamically via Firestore 'feature_flags' collection (SaaSDataService)
export const DEFAULT_FEATURE_FLAGS: FeatureFlag[] = [];
